// Compatibilidade temporária da Fase 2: o caminho legado passa a compartilhar
// a implementação canônica do health no Next, sem duplicar regra HTTP.
export { GET } from '@/app/api/web/health/route'
